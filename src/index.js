

// 验证
const validate = function (source, expect, options) {


    // 先判断是校验一个数组
    const type = typeof source


}


const core = function (value, uses) {

    uses.forEach((use) => {

        const typeUse = Object.prototype.toString().call(use);

        

    });

}


validate.prototype




module.exports = validate;
